import AuthPageWrapper from "@/components/wrappers/AuthPageWrapper";
import React from "react";

export const metadata = {
  title: "ورود به پنل مدیریت | فروشگاه اینترنتی گالری ساعت وینش",
  robots: {
    index: false,
    follow: false,
  },
};

const page = () => {
  return <AuthPageWrapper />;
};

export default page;
